import { Node } from "./node";

function getLevelSum(node, level, runningSum) {}

let tree = Node(5);
tree.left = Node(4);
tree.right = Node(13);

tree.left.left = Node(2);
tree.left.left.left = Node(1);
tree.left.left.right = Node(3);

tree.right.left = Node(12);
