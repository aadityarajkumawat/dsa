#include <bits/stdc++.h>
using namespace std;

class DijkstrasAlgo {
    vector<vector<pair<int, int>>> graph;

    DijkstrasAlgo(vector<vector<pair<int, int>>> _graph) {
        graph = _graph;
    }
};

int main() {
    return 0;
}